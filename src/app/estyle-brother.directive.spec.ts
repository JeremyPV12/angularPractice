import { EstyleBrotherDirective } from './estyle-brother.directive';

describe('EstyleBrotherDirective', () => {
  it('should create an instance', () => {
    const directive = new EstyleBrotherDirective();
    expect(directive).toBeTruthy();
  });
});
